# BG Trip — Friday 2026-06-12 (Classic Era) — Verification Cheat-Sheet

> Every pending in-game verify, consolidated from
> `Research/enemy-threat-research.md`, `ab-node-state-research.md`,
> `wsg-flag-state-research.md`, `bg-detection-reference.md`,
> `dbm-pvp-review-reference.md`, and the pending `SmokeChecklist.md` rows
> (W7, W8, T5, TP2) — in the order you'd run them. Paste/screenshot everything
> to Claude afterwards; the "After the session" list at the bottom is Claude's.
>
> Thanks to the DBM-PvP source review this is **verification, not discovery** —
> most items have an expected answer printed next to them.

---

## 0 · Before queueing (1 minute)

- [ ] `/console scriptErrors 1` (or BugSack active)
- [ ] `/bgthreat` outside any BG → "no enemy scoreboard data yet…", no error
      *(smoke TP1 — already passed 2026-06-10; re-confirm post-login)*
- [ ] Keep this file open on the second screen; screenshots land in `Design/`

---

## 1 · On zone-in — **every** BG, takes ~30 seconds (smoke W7 / W8 / T5)

- [ ] **W8**: window auto-opened by itself (auto-open is on by default)
- [ ] **W7**: the right tab is pre-selected (AB / WSG / AV)
- [ ] **T5**: Titan bar shows the green BG abbreviation (needs "Show label text" on)
- [ ] `/dump select(8, GetInstanceInfo())`
      → **WSG expect `489` · AB expect `529` · AV expect `30`**
      *(ticks the Era rows in `bg-detection-reference.md`)*
- [ ] On leaving the BG: window auto-closes, bar text reverts to icon-only
      (the other half of W8/T5)

---

## 2 · AB match — node state (the `ab-node-state` items)

**On zone-in:**

- [ ] `/dump C_Map.GetBestMapForUnit("player")` → **expect `1461`** (DBM-PvP)
- [ ] All-nodes one-liner (paste as one line):

```
/run local m=C_Map.GetBestMapForUnit("player") for _,id in ipairs(C_AreaPoiInfo.GetAreaPOIForMap(m)) do local p=C_AreaPoiInfo.GetAreaPOIInfo(m,id) print(id,p.name,p.textureIndex) end
```

  → screenshot the 5 lines: `areaPoiID · name · textureIndex` per node

**During the match (event-driven — run the same one-liner again):**

- [ ] right after a node you can see gets **assaulted**
- [ ] right after that node **flips** (~64s later — Era cap time, DBM-PvP)
      → these two snapshots verify the DBM decode table (one flip is enough)
- [ ] Note: did the printed list visibly change at those moments?
      (= `AREA_POIS_UPDATED` delivering)

---

## 3 · Any 15v15 (AB is ideal) — scoreboard shape (the ThreatProvider gate)

**Mid-match, after opening the scoreboard once:**

- [ ] Return-shape dump (one line):

```
/run local n=select("#",GetBattlefieldScore(1)) print("nret",n) for k=1,n do print(k,(select(k,GetBattlefieldScore(1)))) end
```

  → screenshot. **Expect:** position 6 = faction (0/1), **7 = honor rank**,
  10 = classToken, **11 = damageDone, 12 = healingDone**, 17 (talentSpec) = nil

- [ ] Enemy-row sanity: find an index whose position 6 differs from your
      faction; eyeball its name/class/healing (pick the enemy healer you've
      been fighting)
- [ ] `/dump GetNumBattlefieldScores()` → full roster (≈30 in AB) without
      reopening the scoreboard?
- [ ] Event-without-UI check (one line):

```
/run local f=CreateFrame("Frame") f:RegisterEvent("UPDATE_BATTLEFIELD_SCORE") f:SetScript("OnEvent",function() print("UBS",GetNumBattlefieldScores()) end) RequestBattlefieldScoreData()
```

  → "UBS \<n\>" prints with the scoreboard closed = poll loop works

- [ ] `/dump C_PvP and type(C_PvP.GetScoreInfo)` → expected `nil` on Era
      (if a function: screenshot one `/dump C_PvP.GetScoreInfo(1)` too)
- [ ] **TP2**: `/bgthreat` (~3 min in, and again late match) → plausible
      `CC: <healers> | KILL: <dps>` line, local print only
- [ ] Note roughly when enemy healingDone became a clear separator
      (calibrates `HEAL_FLOOR`, currently 20000)

---

## 4 · WSG match — flag events (the `wsg-flag-state` items)

- [ ] Screenshot the **exact** system message for each, as they happen:
      flag **picked up** · **dropped** · **captured** · **returned**
      (both factions if convenient) → becomes the enUS pattern table verbatim,
      and settles DBM's untested-"Unused"-pattern caveat
- [ ] Target/mouseover a flag carrier, then:
      `/dump C_UnitAuras and C_UnitAuras.GetAuraDataByIndex and "C_UnitAuras ok" or "no C_UnitAuras"`
- [ ] Still targeting the FC (one line):

```
/run for i=1,40 do local n,_,_,_,_,_,_,_,_,id=UnitAura("target",i); if n then print(i,n,id) end end
```

  → captures the real flag aura name + spell ID

- [ ] (Low priority, WSG-4) long-held flag: does the carrier show a stacking
      debuff (Focused Assault)? Screenshot if so
- [ ] `/bgthreat` works here too (10v10 — smaller sample, still useful)

---

## 5 · AV (only if a queue pops — nothing is gated on it)

- [ ] Section 1 rows only (W7/W8/T5 + instanceMapID → expect `30`)

---

## After the session (Claude, from your pastes/screenshots)

1. Scoreboard shape → fix `SCORE_POS` in `TitanBgGeneral.lua` **if it
   disagrees**; `enemy-threat-research.md` → Final; TP2 → GREEN; [CMD-6]
   unblocked
2. AB POI snapshots → fill the decode tables; `ab-node-state-research.md` →
   Final; [AB-2/3] unblocked
3. WSG screenshots → enUS pattern table + flag aura IDs;
   `wsg-flag-state-research.md` → Final; [WSG-2/3] unblocked
4. instanceMapID confirms → tick Era rows in `bg-detection-reference.md`
5. Tick smoke rows W7/W8/T5/TP2; update the `CLAUDE.md` research table rows;
   update `project_state.md`
