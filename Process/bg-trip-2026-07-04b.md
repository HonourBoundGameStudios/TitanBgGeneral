# BG Trip — Manual Test Guide (2026-07-04b)

Covers the four items shipped **after** the main 07-04 batch, all committed but
**in-game GREEN pending**: **VERIF-5** (aura half), **VERIF-6** (Details!),
**TEAM-1** (comp engine), **TEAM-2** (Suggested Plan board). Each was
`luac -p`-checked and deployed; TEAM-1's pure logic is harness-verified (28/28),
but nothing here has run in the client. TEAM-2 was built **blind** — eyeball the
look, don't just confirm it works.

**Setup:** already deployed. In-game: `/reload`. Watch for any Lua error on load
(scriptErrors is forced on in dev). **Details! must be loaded** for the VERIF-6
check. Best done in one **Warsong Gulch** match with a quick **Arathi Basin**
peek. Legend: ✅ = expected pass · 🔎 = a research spot-check to confirm/flip.

---

## A. One-time API gate (any BG raid, 10s)

1. **TEAM-1 role API gate** — in a BG (you're in a raid), run:
   `/run for i=1,GetNumGroupMembers() do print((GetRaidRosterInfo(i)), UnitGroupRolesAssigned("raid"..i)) end`
   - 🔎 Does `UnitGroupRolesAssigned` return anything but `"NONE"`? **Expected all
     `"NONE"`** on Era → our healer count stays the class prior (no code change).
     If it returns real roles, note it — we can tighten our healer count later.
   - ✅ The first value each line is a class **token** (`WARRIOR`…), not localized.

## B. Warsong Gulch — the main pass

2. **VERIF-5 aura scan** (the one owed half) — when a flag is carried, **target a
   flag carrier** (an ally **FFC** is easiest — they're on your team), then
   `/reload`. That flushes SavedVariables.
   - 🔎 Afterwards I parse `TitanBgGeneralSaved.Analytics.log.wsg_carrier_aura`:
     it should hold `{ name, spellId (23333/23335), api = "C_UnitAuras"|"UnitAura" }`
     — resolves the last WSG-flag research question. The dev-panel **WSG** LED
     (`/bganalytics panel`) should already be green from the message capture.

3. **VERIF-6 Details! enrichment** — after some fighting, open the panel → **Intel
   table** (or `/bganalytics intel`).
   - ✅ **The tell:** an enemy you *never personally hit* now shows real **Dmg/Heal**
     (Details saw them). Pre-build those rows were **0** unless you'd hit them.
   - ✅ A healer Details tracked shows a confirmed **HEAL** role even if you never
     saw the heal yourself.
   - Degrade check (optional): with Details disabled, the table falls back to the
     CLEU-only numbers — no error either way (guarded).

4. **TEAM-1 `/bgcomp`** — type `/bgcomp` mid-match.
   - ✅ Prints a plan line: `Comp plan [PRESS|STANDARD|TURTLE] <reason> (healers N vs M, diff ±d)`
     plus `FC:`, `Split: O x / D y`, and `Focus:` lines.
   - ✅ Posture matches reality: if you clearly out-heal them → PRESS; out-healed →
     TURTLE. Early game (no CLEU yet) the enemy healer count is the class prior
     (an over-count → leans TURTLE); it should sharpen as the fight develops.
   - ✅ Before the enemy scoreboard loads: a graceful "enemy roster not loaded yet"
     — never a Lua error.

5. **TEAM-2 Suggested Plan board** — open `/bgplan`. **← built blind, eyeball it.**
   - ✅ A **Suggested Plan** section sits at the top: posture in a semantic colour
     (green PRESS / gold STANDARD / orange TURTLE), reason + `(heal N v M, ±d)`,
     a class-coloured **FC** + escort, **Split O/D**, and a **Focus** list, then a
     muted caveat line.
   - 🔎 **Look, don't just function-check:** line spacing/wrapping of the new
     lines, no overlap with the member rows below, window not absurdly tall or
     off-screen. **Grab a screenshot** (WoW window only) for the `ui-ux-reviewer`.
   - ✅ **Broadcast Plan** button sends the plan to BG chat as
     `Plan >> TURTLE: … // FC Druid (…) // O3 D7 // Focus 3x Priest, …` — plain
     text, no stray `|`. (Distinct from the assignment **Broadcast** below it.)
   - ✅ **Refresh** re-pulls rosters and recomputes the plan (this is the
     "live-refine"). `/reload` → the board still opens, plan re-renders.

## C. Arathi Basin — quick peek

6. **TEAM-1/2 in AB** — `/bgcomp` and `/bgplan` in AB.
   - ✅ Posture line uses the **node policy** copy ("hold 3, contest a 4th, mass
     Blacksmith" etc.) — **no** FC line and **no** O/D split (AB is 15v15, handled
     by the `bg` branch).
   - ✅ `/dump GetNumBattlefieldScores()` in a live AB ≈ **30** (15/side) — confirms
     the AB roster-size assumption.

---

## Regression sweep (must still pass)

- ✅ Addon loads with **no Lua error**; existing panels/tabs unchanged.
- ✅ `/bgplan` member rows, Openers, Markers, and the assignment Broadcast still
  work (GroupMembers was lifted to file scope — same behaviour, one source now).
- ✅ `/bgthreat`, `/bganalytics`, the Intel overlay, AB strip / WSG FC band all
  unchanged.

## When done

- VERIF-5: mark the aura row confirmed in `Research/wsg-flag-state-research.md`
  (+ note which aura API), flip the doc toward **Final**.
- VERIF-6 / TEAM-1: flip their "GREEN-pending" notes in `Backlog.md`; record the
  `UnitGroupRolesAssigned` result in the TEAM-1 note + research Action Items.
- TEAM-2: run the `ui-ux-reviewer` on the `/bgplan` screenshot; fix every
  Blocker/Major and re-review until PASS, then flip its note.
- Any failure returns as a new `[BUG]` in `Backlog.md`.
